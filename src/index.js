// main code
// String.fromCharCode()
function char(n) {
  // wher n comes from a generator
  console.log(n + ' is ' + String.fromCharCode(n));
}

function generatePW(length, lower, upper, nums, specials) {}
